#!/usr/bin/env bash

sudo add-apt-repository ppa:neovim-ppa/stable 
sudo add-apt-repository ppa:git-core/ppa
sudo apt-get update

sudo apt-get install \
    neovim git fasd fzf ripgrep tldr neomutt

sudo apt autoremove
